import { ProfileState } from '../../Interface/Profile';

const profileInitialState: ProfileState = {
    userProfileState: null,
    favouriteShopsState: null,
};

export default profileInitialState;
